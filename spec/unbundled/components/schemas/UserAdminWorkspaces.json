{
  "type": "object",
  "properties": {
    "id": {
      "type": "integer"
    },
    "name": {
      "type": "string"
    },
    "permissions": {
      "type": "string",
      "description": "The permissions that the user has within the workspace.",
      "maxLength": 32
    }
  },
  "required": [
    "id",
    "name"
  ]
}